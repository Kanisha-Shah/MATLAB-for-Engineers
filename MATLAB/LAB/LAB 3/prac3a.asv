clc;
clear all;
close all;

s = 'Enter your volts value';
volts = input(s);

if volts < 105
    disp('WARNING: Low voltage on line.');
    if volts > 125
        disp('WARNING: High voltage on line.');
    else
        disp('Line voltage is within tolerances.');
    end
end
