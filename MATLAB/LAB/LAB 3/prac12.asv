clc;
clear all;
close all;

s = 'Enter temp value';
temp = input(s);

if temp <= 97.5
    disp("Below Normal");
elseif temp > 97.5 && temp < 99.5
    disp("Normal");
elseif temp >= 99.5 && temp < 103.0
    disp("Slightly High");
elseif temp >= 103.0
    disp("Dangerously High");
end