clc;
clear all;
close all;

while(true)
    s = 'Enter x value';
    x = input(s);
    if x < 1.0
        y = log(1/(1-x));
        fprintf("Y is: %f",y)
        break
    
    else
        disp("Invalid Input")  
    end
end
