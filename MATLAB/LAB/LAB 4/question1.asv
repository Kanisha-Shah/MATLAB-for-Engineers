clc;
clear all;
close all;

x = -1:0.1:3;
for i = -1:0.1:3
    y = x.*x - 3.*x + 2;
end
disp(y);
plot(x,y,"LineStyle","--","Color","red","LineWidth",3);

figure
x1 = -1:0.1:3;
y1 = x.*x - 3.*x + 2;

disp(y1);
plot(x1,y1,"LineStyle",":","Color","blue","LineWidth",1);
